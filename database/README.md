# Database Structure

This directory contains resources for initializing and managing the database.

- `migrations/`: Contains Go-migrate up and down SQL scripts.
- `seed/`: Contains seed data for development and testing.
