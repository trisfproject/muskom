/**
 * Candidate — Candidate profile card grid.
 *
 * Phase 1: Empty stub. Implementation pending Phase 2.
 *
 * Note: Fetches data from `GET /api/v1/public/candidates` via landingService.
 */
export function Candidate() {
  // TODO Phase 2: implement candidate card grid
  return null
}
